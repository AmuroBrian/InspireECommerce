export default function LayoutSecond({ children }) {
    return (
      <html lang="en">
        <body className="w-full h-screen flex bg-[#daf1ff] text-black">
          {children}
        </body>
      </html>
    );
  }
  